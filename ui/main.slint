import { ListView } from "std-widgets.slint";

export component MainWindow inherits Window {
    width : 300px;
    height : 200px;
    title : "Torch Asset Manager";

    in property <[string]> folders;
    
    ListView {
        for folder in folders : Text {
            text : folder;
        }
    }
}